#!/bin/sh
set -e

# Stop any running kDrive instances before removal
if command -v killall > /dev/null 2>&1; then
    killall kDrive 2>/dev/null || true
    killall kDrive_client 2>/dev/null || true
fi

exit 0
